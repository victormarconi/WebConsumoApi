@{
    ViewData["Title"] = "Home Page";
}


<div class="row">
    <div class="col-sm-12">

        <div class="card">
            <div class="card-header"> Cargar Excel</div>
            <div class="card-body">

                <div class="row">
                    <div class="col-sm-4">
                         <input class="form-control-file" type="file" id="inputExcel" />
                    </div>
                    <div class="col-sm-4">
                         <button class="btn btn-warning" onclick="mostrarDados()">Mostrar Dados</button>
                         <button class="btn btn-success" onclick="enviarDados()">Enviar Dados a BD</button>
                    </div>
                </div>

                <hr />

                <div class="row">
                    <div class="col-sm-12">
                        <table id="tbData" class="table table-striped">
                            <thead>
                                <tr>
                                    <th>name</th>
                                    <th>Sku</th>
                                </tr>
                            </thead>
                            <tbody>

                            </tbody>
                        </table>
                    </div>
                </div>

            </div>
        </div>

    </div>
</div>

@section Scripts{

    <script>

        function mostrarDados(){
            const input = document.getElementById("inputExcel")

            const formData = new FormData()

            formData.append("ArquivoExcel",input.files[0])

            fetch("Home/MostrarDados",{
                method: "POST",
                body:formData
            })
            .then((response) => { return response.json()})
            .then((dataJson) => {
                
                
                dataJson.forEach((item) => {
                    $("#tbData tbody").append(
                        $("<tr>").append(
                            $("<td>").text(item.name),
                            $("<td>").text(item.sku),
                         /*   $("<td>").text(item.description),
                            $("<td>").text(item.price),
                            $("<td>").text(item.qty),
                            $("<td>").text(item.ean),
                            $("<td>").text(item.sku_manufacturer),
                            $("<td>").text(item.net_weight),
                            $("<td>").text(item.gross_weight),
                            $("<td>").text(item.width),
                            $("<td>").text(item.height),
                            $("<td>").text(item.depth),
                            $("<td>").text(item.guarantee),
                            $("<td>").text(item.ncm),
                            $("<td>").text(item.manufacturer),
                            $("<td>").text(item.category),
                            $("<td>").text(item.images), */
                        )
                    
                    )
                })
            
            })
        
        
        }


        function enviarDados(){
            const input = document.getElementById("inputExcel")

            const formData = new FormData()

            formData.append("ArquivoExcel",input.files[0])

            fetch("Home/EnviarDados",{
                method: "POST",
                body:formData
            })
            .then((response) => { return response.json()})
            .then((dataJson) => {

              alert(dataJson.mensaje);
            
            })
        
        
        }

    </script>

}